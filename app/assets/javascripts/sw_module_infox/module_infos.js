// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
$(function() {
	$( "#module_info_submit_date" ).datepicker({format:'yy-mm-dd'});
	$( "#module_info_start_date_s" ).datepicker({dateFormat: 'yy-mm-dd'});
	$( "#module_info_end_date_s" ).datepicker({dateFormat: 'yy-mm-dd'});

});